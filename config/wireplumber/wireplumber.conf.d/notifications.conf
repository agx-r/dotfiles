context.modules = [
    { name = libpipewire-module-loopback
        args = {
            node.description = "System Notifications"
            capture.props = {
                node.name = "notifications_input"
                media.class = "Audio/Sink"
                audio.position = [ FL FR ]
            }
            playback.props = {
                node.name = "notifications_output"
                node.passive = true
                target.object = "auto"
            }
        }
    }
]
